version: 2

sources:
  - name: jaffle_shop_duckdb
    schema: main # This is the schema DuckDB created for our seeds
    tables:
      - name: raw_customers
      - name: raw_orders
      - name: raw_payments
